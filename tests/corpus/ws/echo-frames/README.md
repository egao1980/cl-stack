# ws/echo-frames

MIT synthetic WebSocket echo / framing vectors for wave-1 `ws-protocol`.

- `vectors.lisp` — plists (`:id`, `:opcode`, `:payload`, …)
- Not copied from upstream suites; regenerate freely under MIT.
